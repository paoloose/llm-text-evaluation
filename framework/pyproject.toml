[project]
name = "llm-verbal-framework"
version = "0.1.0"
description = "A framework for evaluating LLM robustness on verbal reasoning tasks in Spanish."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.39",
    "openai>=1.30",
    "pydantic>=2.0",
]
